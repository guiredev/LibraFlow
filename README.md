# LibraFlow

Sistema PHP/MySQL de gestao de biblioteca.

Comece pelo `HANDOFF.md` para entender a estrutura atual. As paginas acessadas pelo navegador ficam em `public/`, e o codigo compartilhado fica em `app/`.